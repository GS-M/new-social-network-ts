
import './index.css';
import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import { AppContainer } from './App';


const root = ReactDOM.createRoot(document.getElementById('root'));

root.render(
    <React.StrictMode>
        <AppContainer />
    </React.StrictMode>
);

// В данном проекте одинаковые задачи могут выполняться по разному,
// чтобы показать возможность их решения разными способами
